import React from "react";
import {LoadContainer} from "../../styles/component"
const Loading = () => (
    <LoadContainer>
        <span role="img" aria-label="loading">
            🤖 🎶 
        </span>
    </LoadContainer>
  );

export default Loading;